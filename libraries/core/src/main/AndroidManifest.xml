<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="flavoury.libraries.core">

    <application>
        <provider
            android:name="flavoury.libraries.core.CoreInitProvider"
            android:authorities="${applicationId}.coreinitprovider"
            android:exported="false" />
    </application>

</manifest>
